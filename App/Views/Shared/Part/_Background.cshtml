@model string

<background>
    <div class="flex fixed pointer-events-none bottom-10 right-10 -z-10">
        <div class="fill-neutral-800 opacity-20">
            @await Html.PartialAsync(@Model)
        </div>
    </div>
</background>